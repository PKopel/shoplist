package com.example.shoplist.data.model.items

import com.example.shoplist.R

enum class Shop(val label: Int) {
    Small(R.string.tab_small),Market(R.string.tab_market),Special(R.string.tab_special)
}